// const blacklistedTokens = [];

// export const addToken = (token) => {
//   console.log("Agregando token a la lista negra:", token);
//   blacklistedTokens.push(token);
// };

// export const isBlacklisted = (token) => {
//   return blacklistedTokens.includes(token);
// };
